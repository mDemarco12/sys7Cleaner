
[package]
name = "system7-cleaner"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "system7_cleaner"
crate-type = ["staticlib", "cdylib", "rlib"]
